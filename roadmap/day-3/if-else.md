- Khi conditional đúng thì mới chạy vào body code
- Conditional ở đây thường là toán tử so sánh và toán tử logic

```js
if (conditional) {
  // body code
} else if (conditional) {
  // body code
} else {
  // body code
}
```
